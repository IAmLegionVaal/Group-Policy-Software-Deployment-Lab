# Completed Remediation

The first validation did not produce the intended result.

The configuration location, access settings and policy scope were corrected. The affected endpoint configuration was refreshed and the validation was repeated. The intended endpoint then completed the deployment, while systems outside the selected scope remained unchanged.

**This was tested by me to be working. User experience may vary.**
